graphit:
  url: "http://localhost:8888/"

auth:
  url: https://xxxx:9443/oauth2/token
  clientId: xxxxxxxxxxxxxxxxxxxxxxxxxxxx
  clientSecret: xxxxxxxxxxxxxxxxxxxxxxxxxxxx
  username:
  passwd:

aws:
  AWS_ACCESS_KEY:
  AWS_SECRET_KEY:
  sqs-url: https://sqs.eu-west-1.amazonaws.com/xxxxxxxx/ec2-monitoring-events
  sqs-timeout: 30
  sqs-messages: 100

skip-certs-validation: false

model:
  # used to create issue on Machine naode prefix+aws-instance-id
  machine-node-prefix:
  # used to create issue if can not match aws-instance-id
  default-node-id: